# Notebooks

`AGML-DenseCBAM-Training.ipynb` is a thin interface to the active scripts. Launch Jupyter from the repository root and select the `thesis-env` kernel:

```bash
jupyter lab notebooks/AGML-DenseCBAM-Training.ipynb
```

The notebook locates the project root automatically. Training and analysis logic remains in `scripts/` to avoid notebook/script drift.
